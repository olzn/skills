# Learnings — hig-design

Local runtime notes. The installer preserves this file across updates. Append short, dated, reusable findings (generation pitfalls, medium quirks, project conventions that interact with native idiom) — not session narration.
